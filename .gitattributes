# Generated data artifacts can contain intentional trailing spaces inside
# quoted model transcripts. Keep code/docs whitespace checks enabled.
data/**/*.csv -whitespace
data/**/*.jsonl -whitespace
runs/**/*.csv -whitespace
runs/**/*.jsonl -whitespace

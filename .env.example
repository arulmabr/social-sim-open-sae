# Required only for GPT judge reruns.
OPENAI_API_KEY=

# Required only for Open-SAE GPU reruns using gated Meta Llama weights.
HF_TOKEN=

# Historical Goodfire Ember API is not required for this repo. The old hosted
# endpoint is currently unavailable, so labels come from Neuronpedia/Open-SAE.
